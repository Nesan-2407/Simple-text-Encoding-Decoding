# Simple-text-Encoding-Decoding
This CLI tool implements the Caesar Cipher for encoding/decoding text. It uses character-level manipulation (ord/chr) and modular arithmetic to shift letters, handling wrap-around (Z to A). The project demonstrates O(n) string processing, modular design, and input validation.
